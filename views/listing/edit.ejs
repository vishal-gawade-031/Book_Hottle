<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WonderLust</title>
</head>
<body>
    
    <h1>Add new details to Edit Listing</h1>
    <!-- convert the post request into put with install the method override package -->
    <form method="POST" action="/listings/<%= listing._id %>?_method=PUT">

        <!-- display the information from DB -->

        <input type="text" name="listing[title]" value="<%= listing.title %>">
        <br><br>
        <textarea type="text" name="listing[description]"><%= listing.description %></textarea>
        <br><br>
        <input  name="listing[image]" value="<%=listing.image %>">
        <br><br>
        <input type="number" name="listing[price]" value="<%= listing.price %>">
        <br><br>
        <input type="text" name="listing[country]" value="<%= listing.country %>">
        <br><br>
        <input type="text" name="listing[location]" value="<%= listing.location %>">
        <br><br>

        <button>Edit</button>
    </form>
</body>
</html>